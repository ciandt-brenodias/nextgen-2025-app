export interface UserData {
    name: string;
    skill: string;
  }

export interface UserToken {
    exp: string,
    grant_type: string,
    id: string,
    nome: string,
    documento: string,
    perfil: string,
    email: string,
  }